const DB_USER = 'user';
const DB_PASSWORD = 'user';
const DB_HOST = 'cluster0.vfnoc.mongodb.net';

const API_VERSION = "v1";
const IP_SERVER ='locahost';

module.exports = {
    DB_USER,
    DB_PASSWORD,
    DB_HOST,
    API_VERSION,
    IP_SERVER
}